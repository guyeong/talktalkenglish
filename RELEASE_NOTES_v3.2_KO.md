# TalkTalk English v3.2

## 감정 낭독

- 낭독 스타일 선택 추가: `또렷하게`, `동화책처럼`, `연극처럼`
- 따옴표 안의 대화와 따옴표 밖의 내레이션을 음성에서 분명하게 구분
- 물음표, 느낌표와 `whispered`, `shouted`, `laughed`, `afraid` 같은 표현을 이용해 호기심·긴장·기쁨·속삭임 등을 자동 반영
- 전체 읽기와 문장 터치에는 선택한 감정 낭독 스타일 적용
- 따라 읽기와 따라 읽기 평가는 발음 학습을 위해 항상 `또렷하게` 스타일 사용
- 단어 터치는 감정 없이 표준 발음 유지

## OCR 속도 유지 및 개선

- 감정 판별을 OCR 결과에 추가하지 않았습니다. OCR 요청 횟수와 JSON 구조는 그대로이므로 감정 낭독 때문에 OCR이 느려지지 않습니다.
- 감정 지시는 음성을 만드는 순간 TTS 요청 안에서 처리하며 별도의 감정 분석 API를 호출하지 않습니다.
- OCR 모델은 저지연 문서 추출용 안정 모델 `gemini-3.5-flash-lite`로 갱신했습니다.
- 이미 분석한 페이지는 기존처럼 IndexedDB 결과를 재사용합니다.
